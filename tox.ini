[tox]
envlist = 
    py36
    py37
    py38

[testenv]
deps = 
    .[tests]
    pytest
    pytest-cov
    
commands = 
    coverage run --include digitalocean-inventory/* -m    \
    pytest --doctest-modules digitalocean-inventory    

    coverage run --include digitalocean-inventory/* -a -m \
    pytest

    coverage report
    coverage xml
